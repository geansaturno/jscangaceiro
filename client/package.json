{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --out-dir app --source-maps",
    "watch": "babel src --out-dir app --source-maps --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "systemjs": "^6.4.1"
  },
  "devDependencies": {
    "@babel/cli": "^7.10.5",
    "@babel/core": "^7.11.1",
    "@babel/plugin-transform-modules-systemjs": "^7.10.5"
  }
}
